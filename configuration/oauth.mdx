---
title: 'OAuth'
description: 'Fully working OAuth flow without ant external provider + Magic Links'
icon: 'right-to-bracket'
---

## Configuration

The OAuth does not depends on any external providers. It's build using the `golang.org/x/oauth2` library. 
The base aplication already includes configurations for two providers; Google and GitHub.
